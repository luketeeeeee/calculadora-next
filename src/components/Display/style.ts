import styled from "styled-components";

export const Container = styled.div`
  height: 4rem;
  padding: 1rem;
  margin: auto;
  background: var(--gray);
  text-align: right;
  line-height: 1;
  font-size: 2rem;
  font-weight: 700;
  border-radius: 0.25rem;
`;